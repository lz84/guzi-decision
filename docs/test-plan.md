# 谷子情报分析系统 - 测试方案与测试计划

## 文档信息
- **项目名称**: 谷子情报分析系统
- **文档类型**: 测试方案与测试计划
- **版本**: 1.0
- **创建日期**: 2026-03-17
- **创建者**: 麻子 (Paperclip Agent)
- **Issue**: MAK-8 - 基于需求分析进一步设计

---

## 第一部分：测试方案

### 一、测试范围

#### 1.1 测试内容

| 测试类型 | 测试内容 | 优先级 |
|----------|----------|--------|
| 功能测试 | 所有功能模块的功能点 | P0 |
| 性能测试 | API 响应时间、并发处理能力 | P1 |
| 准确性测试 | 情感分析、事件提取准确率 | P0 |
| 安全测试 | API 认证、数据安全 | P1 |
| 兼容性测试 | 不同平台数据采集兼容性 | P2 |
| 可靠性测试 | 系统稳定性、容错能力 | P1 |

#### 1.2 不测试内容

| 排除项 | 原因 |
|--------|------|
| 第三方 API 内部测试 | 依赖第三方服务质量 |
| 硬件兼容性测试 | 单机部署 |
| 国际化测试 | 仅支持中文 |

### 二、测试策略

#### 2.1 测试层次

```
┌─────────────────────────────────────────────────────────────────┐
│                       测试金字塔                                 │
└─────────────────────────────────────────────────────────────────┘

                        ▲
                       /│\
                      / │ \
                     /  │  \    E2E 测试 (10%)
                    /   │   \   - 完整业务流程
                   /    │    \
                  /─────┼─────\
                 /      │      \
                /       │       \  集成测试 (20%)
               /        │        \ - 模块间接口
              /         │         \
             /──────────┼──────────\
            /           │           \
           /            │            \ 单元测试 (70%)
          /             │             \ - 函数、类方法
         /──────────────┼──────────────\
```

#### 2.2 测试方法

| 测试类型 | 方法 | 工具 |
|----------|------|------|
| 单元测试 | 白盒测试 | pytest, unittest |
| 集成测试 | 灰盒测试 | pytest, requests |
| E2E 测试 | 黑盒测试 | playwright, requests |
| 性能测试 | 负载测试 | locust, ab |
| 准确性测试 | 评估测试 | 人工评估 + 自动评估 |

### 三、测试环境

#### 3.1 环境配置

| 环境 | 配置 | 用途 |
|------|------|------|
| 开发环境 | 本地开发机 | 开发调试 |
| 测试环境 | 单机服务器 | 功能测试、集成测试 |
| 生产环境 | 单机服务器 | 生产运行 |

#### 3.2 测试环境配置

```
硬件配置:
- CPU: 4 核
- 内存: 16 GB
- 硬盘: 500 GB SSD

软件配置:
- OS: Ubuntu 22.04 / Windows 10+
- Python: 3.10+
- MongoDB: 6.0
- Redis: 7.0
- Milvus: 2.3
```

### 四、测试数据

#### 4.1 测试数据来源

| 数据类型 | 来源 | 数量 | 说明 |
|----------|------|------|------|
| Twitter 数据 | 模拟数据 | 1000 条 | 包含多种情感倾向 |
| Reddit 数据 | 模拟数据 | 500 条 | 多主题讨论 |
| 新闻数据 | 模拟数据 | 200 条 | 财经新闻 |
| 已标注情感数据 | 人工标注 | 500 条 | 正/负/中性各占约 1/3 |
| 已标注事件数据 | 人工标注 | 100 条 | 多类型事件 |
| 已标注实体数据 | 人工标注 | 200 条 | 人名/地名/机构 |

#### 4.2 测试数据分布

```
情感标注数据分布：
├── 正面情感：180 条 (36%)
├── 负面情感：190 条 (38%)
└── 中性情感：130 条 (26%)

事件类型分布：
├── 政治事件：30 条 (30%)
├── 经济事件：25 条 (25%)
├── 社会事件：25 条 (25%)
└── 其他事件：20 条 (20%)

实体类型分布：
├── 人名 (PERSON)：80 条 (40%)
├── 地名 (GPE)：60 条 (30%)
└── 机构 (ORG)：60 条 (30%)
```

#### 4.3 测试数据生成脚本

```python
import random
from datetime import datetime, timedelta
from typing import List, Dict

def generate_test_tweets(count: int = 1000) -> List[Dict]:
    """生成测试推文数据"""
    sentiments = ["positive", "negative", "neutral"]
    templates = [
        "关于{topic}的讨论，我觉得{opinion}",
        "{entity}发布了重要消息：{content}",
        "最新动态：{event}已经发生",
    ]
    
    test_data = []
    for i in range(count):
        sentiment = random.choice(sentiments)
        test_data.append({
            "id": f"test_tweet_{i}",
            "content": generate_content(sentiment),
            "sentiment": sentiment,
            "entities": generate_entities(),
            "created_at": datetime.now() - timedelta(minutes=i),
            "platform": "twitter"
        })
    return test_data

def generate_labeled_sentiment_data(count: int = 500) -> List[Dict]:
    """生成已标注情感数据"""
    # 从真实数据中抽样，人工标注
    pass
```

#### 4.4 测试数据存储位置

```
tests/
├── data/
│   ├── raw/                    # 原始测试数据
│   │   ├── tweets.json
│   │   ├── reddit_posts.json
│   │   └── news_articles.json
│   ├── labeled/                # 已标注数据
│   │   ├── sentiment_labels.json
│   │   ├── event_labels.json
│   │   └── entity_labels.json
│   └── expected/               # 预期结果
│       ├── sentiment_expected.json
│       └── event_expected.json
└── fixtures/                   # 测试固件
    ├── mock_collector.py
    └── mock_analyzer.py
```

### 四-B、测试环境详细配置

#### 4B.1 测试环境架构

```
┌─────────────────────────────────────────────────────────────────┐
│                       测试环境架构                               │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      测试服务器                                  │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐  │
│  │ API服务 │ │调度服务 │ │ MongoDB │ │  Redis  │ │ Milvus  │  │
│  │ :8000   │ │ (后台)  │ │ :27017  │ │ :6379   │ │ :19530  │  │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘  │
│                                                                 │
│  配置：                                                         │
│  - CPU: 4 核                                                    │
│  - 内存: 16 GB                                                  │
│  - 硬盘: 500 GB SSD                                             │
│  - OS: Ubuntu 22.04 / Windows 10+                               │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      测试客户端                                  │
│  - pytest 执行环境                                              │
│  - Locust 压力测试                                              │
│  - 人工测试浏览器                                               │
└─────────────────────────────────────────────────────────────────┘
```

#### 4B.2 环境配置差异

| 配置项 | 测试环境 | 生产环境 | 差异说明 |
|--------|----------|----------|----------|
| 数据库规模 | 1 万条 | 100 万条+ | 测试数据量小 |
| 并发用户 | 10 | 100+ | 测试并发低 |
| LLM 调用 | Mock | 真实 API | 测试用 Mock |
| 外部服务 | Mock | 真实服务 | 测试隔离外部依赖 |
| 日志级别 | DEBUG | INFO | 测试日志详细 |

#### 4B.3 Mock 服务配置

```python
# tests/fixtures/mock_services.py

class MockLLMService:
    """模拟 LLM 服务"""
    
    async def analyze_sentiment(self, text: str) -> dict:
        # 基于规则的模拟情感分析
        negative_words = ["差", "坏", "失败", "问题"]
        positive_words = ["好", "棒", "成功", "优秀"]
        
        text_lower = text.lower()
        if any(w in text_lower for w in negative_words):
            return {"label": "negative", "score": -0.6}
        elif any(w in text_lower for w in positive_words):
            return {"label": "positive", "score": 0.6}
        return {"label": "neutral", "score": 0.0}

class MockCollectorService:
    """模拟采集服务"""
    
    async def collect(self, source: str, keywords: list) -> list:
        # 返回预定义的测试数据
        return load_test_data(f"data/raw/{source}_test.json")
```

#### 4B.4 环境初始化脚本

```bash
#!/bin/bash
# tests/scripts/init_test_env.sh

# 启动测试数据库
docker-compose -f docker-compose.test.yml up -d mongodb redis milvus

# 等待服务就绪
sleep 10

# 初始化测试数据
python tests/scripts/init_test_data.py

# 运行健康检查
curl -s http://localhost:8000/health || exit 1

echo "测试环境初始化完成"
```

---

## 五、验收标准

#### 5.1 功能验收标准

| 功能模块 | 验收标准 | 测试方法 |
|----------|----------|----------|
| 数据采集 | 所有配置的数据源可正常采集 | 自动化测试 |
| 情感分析 | 准确率 > 80% | 标注数据评估 |
| 事件提取 | 准确率 > 70% | 标注数据评估 |
| 预警服务 | 预警延迟 < 1 分钟 | 压力测试 |
| API 服务 | 所有 API 正常响应 | 接口测试 |

#### 5.2 性能验收标准

| 指标 | 验收标准 | 测试方法 |
|------|----------|----------|
| API 响应时间 | P95 < 500ms | Locust 压力测试 |
| 并发处理 | > 100 QPS | Locust 压力测试 |
| 系统可用性 | > 99% | 监控统计 |

#### 5.3 量化验收标准详情

| 验收项 | 指标要求 | 通过标准 | 测试数据量 |
|--------|----------|----------|------------|
| 功能测试通过率 | ≥ 95% | ≥ 95% 用例通过 | 全部用例 |
| 代码覆盖率 | ≥ 80% | 行覆盖率 ≥ 80% | 全部代码 |
| 情感分析准确率 | ≥ 80% | F1-Score ≥ 0.78 | 500 条标注数据 |
| 事件提取准确率 | ≥ 70% | F1-Score ≥ 0.68 | 100 条标注数据 |
| 实体识别准确率 | ≥ 85% | F1-Score ≥ 0.83 | 200 条标注数据 |
| 去重准确率 | ≥ 95% | Precision ≥ 0.95 | 1000 条数据 |
| API 响应时间 P95 | < 500ms | 95% 请求 < 500ms | 1000 次请求 |
| 并发处理能力 | > 100 QPS | 无错误响应 | 持续 5 分钟 |
| 系统可用性 | > 99% | 30 天内故障 < 7.2 小时 | 监控统计 |
| 数据采集成功率 | > 95% | 成功采集数/总任务数 | 100 次任务 |

---

## 第二部分：测试计划

### 六、测试进度安排

#### 6.1 总体计划

| 阶段 | 时间 | 内容 | 产出物 |
|------|------|------|--------|
| 测试准备 | 第 1 周 | 测试环境、测试数据、测试用例 | 测试计划、测试用例 |
| 单元测试 | 第 2 周 | 各模块单元测试 | 单元测试报告 |
| 集成测试 | 第 3 周 | 模块间接口测试 | 集成测试报告 |
| 系统测试 | 第 4 周 | 功能、性能、安全测试 | 系统测试报告 |
| 验收测试 | 第 5 周 | 用户验收测试 | 验收测试报告 |

#### 6.2 详细进度

| 日期 | 任务 | 负责人 | 状态 |
|------|------|--------|------|
| 2026-03-17 | 编写测试计划 | 麻子 | 进行中 |
| 2026-03-18 | 准备测试数据 | 麻子 | 待开始 |
| 2026-03-19 | 编写测试用例 | 麻子 | 待开始 |
| 2026-03-20 | 搭建测试环境 | 麻子 | 待开始 |

### 七、测试用例设计

#### 7.1 功能测试用例

##### TC-001: 数据采集测试

| 用例编号 | TC-001 |
|----------|--------|
| 用例名称 | Twitter 数据采集测试 |
| 前置条件 | agent-reach 技能可用 |
| 测试步骤 | 1. 配置 Twitter 关键词<br>2. 启动采集任务<br>3. 等待采集完成<br>4. 检查采集结果 |
| 预期结果 | 采集到符合关键词的推文数据 |
| 优先级 | P0 |

##### TC-002: 情感分析测试

| 用例编号 | TC-002 |
|----------|--------|
| 用例名称 | 情感分析准确率测试 |
| 前置条件 | 已准备已标注测试数据 500 条 |
| 测试步骤 | 1. 加载测试数据<br>2. 执行情感分析<br>3. 对比分析结果与标注<br>4. 计算准确率 |
| 预期结果 | 准确率 > 80% |
| 优先级 | P0 |

##### TC-003: 事件提取测试

| 用例编号 | TC-003 |
|----------|--------|
| 用例名称 | 事件提取准确率测试 |
| 前置条件 | 已准备已标注事件数据 100 条 |
| 测试步骤 | 1. 加载测试数据<br>2. 执行事件提取<br>3. 对比提取结果与标注<br>4. 计算准确率 |
| 预期结果 | 准确率 > 70% |
| 优先级 | P0 |

##### TC-004: 预警触发测试

| 用例编号 | TC-004 |
|----------|--------|
| 用例名称 | 预警触发及时性测试 |
| 前置条件 | 预警规则已配置 |
| 测试步骤 | 1. 输入触发预警的测试数据<br>2. 记录输入时间<br>3. 等待预警发送<br>4. 记录预警接收时间 |
| 预期结果 | 预警延迟 < 1 分钟 |
| 优先级 | P0 |

##### TC-005: API 接口测试

| 用例编号 | TC-005 |
|----------|--------|
| 用例名称 | 情感数据 API 测试 |
| 前置条件 | API 服务已启动 |
| 测试步骤 | 1. 调用 GET /api/v1/sentiment/latest<br>2. 验证响应状态码<br>3. 验证响应数据格式<br>4. 验证数据正确性 |
| 预期结果 | 状态码 200，数据格式正确 |
| 优先级 | P0 |

#### 7.2 性能测试用例

##### PT-001: API 响应时间测试

| 用例编号 | PT-001 |
|----------|--------|
| 用例名称 | 情感 API 响应时间测试 |
| 前置条件 | 系统正常运行 |
| 测试步骤 | 1. 使用 locust 发起请求<br>2. 持续 5 分钟<br>3. 统计响应时间分布 |
| 预期结果 | P95 响应时间 < 500ms |
| 工具 | locust |

##### PT-002: 并发处理测试

| 用例编号 | PT-002 |
|----------|--------|
| 用例名称 | 系统并发处理能力测试 |
| 前置条件 | 系统正常运行 |
| 测试步骤 | 1. 使用 locust 模拟并发用户<br>2. 逐步增加用户数<br>3. 观察系统响应 |
| 预期结果 | 支持 > 100 QPS |

#### 7.3 准确性测试用例

##### AT-001: 情感分析准确率测试

| 用例编号 | AT-001 |
|----------|--------|
| 用例名称 | 情感分析模型准确率评估 |
| 测试数据 | 已标注情感数据 500 条 |
| 评估指标 | Precision, Recall, F1-Score |
| 预期结果 | F1-Score > 0.80 |

##### AT-002: 事件提取准确率测试

| 用例编号 | AT-002 |
|----------|--------|
| 用例名称 | 事件提取模型准确率评估 |
| 测试数据 | 已标注事件数据 100 条 |
| 评估指标 | Precision, Recall, F1-Score |
| 预期结果 | F1-Score > 0.70 |

##### AT-003: 实体识别准确率测试

| 用例编号 | AT-003 |
|----------|--------|
| 用例名称 | 实体识别模型准确率评估 |
| 测试数据 | 已标注实体数据 200 条 |
| 评估指标 | Precision, Recall, F1-Score |
| 预期结果 | F1-Score > 0.83 |

##### AT-004: 去重准确率测试

| 用例编号 | AT-004 |
|----------|--------|
| 用例名称 | 去重准确率评估 |
| 测试数据 | 已知重复数据对 500 对 |
| 评估指标 | Precision, Recall |
| 预期结果 | Precision > 95%, Recall > 90% |

#### 7.4 兼容性测试用例

##### CT-001: 多平台采集兼容性测试

| 用例编号 | CT-001 |
|----------|--------|
| 用例名称 | Twitter 平台兼容性测试 |
| 前置条件 | agent-reach 技能可用 |
| 测试步骤 | 1. 配置 Twitter 采集任务<br>2. 执行采集<br>3. 验证数据格式<br>4. 验证字段完整性 |
| 预期结果 | 数据格式正确，字段完整 |
| 优先级 | P0 |

##### CT-002: 不同平台数据格式兼容性

| 用例编号 | CT-002 |
|----------|--------|
| 用例名称 | 多平台数据格式兼容性测试 |
| 测试平台 | Twitter, Reddit, 新闻网站 |
| 测试步骤 | 1. 从各平台采集测试数据<br>2. 验证统一格式转换<br>3. 验证字段映射正确 |
| 预期结果 | 所有平台数据格式统一 |

#### 7.5 安全测试用例

##### ST-001: API 认证测试

| 用例编号 | ST-001 |
|----------|--------|
| 用例名称 | API 认证机制测试 |
| 测试步骤 | 1. 无 Token 访问 API<br>2. 无效 Token 访问 API<br>3. 有效 Token 访问 API |
| 预期结果 | 无 Token 返回 401，无效 Token 返回 401，有效 Token 返回 200 |

##### ST-002: 数据访问权限测试

| 用例编号 | ST-002 |
|----------|--------|
| 用例名称 | 数据访问权限控制测试 |
| 测试步骤 | 1. 以普通用户身份访问敏感数据<br>2. 以管理员身份访问敏感数据<br>3. 验证权限隔离 |
| 预期结果 | 普通用户无法访问敏感数据 |

##### ST-003: 数据脱敏测试

| 用例编号 | ST-003 |
|----------|--------|
| 用例名称 | 敏感数据脱敏测试 |
| 测试步骤 | 1. 检查存储的用户ID是否脱敏<br>2. 检查日志中的敏感信息<br>3. 检查 API 返回数据 |
| 预期结果 | 敏感数据均已脱敏处理 |

##### ST-004: 注入攻击防护测试

| 用例编号 | ST-004 |
|----------|--------|
| 用例名称 | SQL/NoSQL 注入防护测试 |
| 测试步骤 | 1. 在查询参数中注入恶意代码<br>2. 验证系统响应 |
| 预期结果 | 恶意代码被过滤，无异常 |

### 八、测试执行

#### 8.1 测试执行流程

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│ 测试准备    │ ──► │ 测试执行    │ ──► │ 缺陷管理    │
└─────────────┘     └─────────────┘     └─────────────┘
                           │                   │
                           │                   ▼
                      ┌─────────────┐     ┌─────────────┐
                      │ 测试报告    │ ◄── │ 回归测试    │
                      └─────────────┘     └─────────────┘
```

#### 8.2 缺陷管理

| 缺陷等级 | 定义 | 处理时限 |
|----------|------|----------|
| Critical | 系统崩溃、数据丢失 | 24 小时 |
| Major | 功能缺失、性能严重下降 | 3 天 |
| Minor | 功能异常但有替代方案 | 1 周 |
| Trivial | 界面问题、优化建议 | 低优先级 |

#### 8.3 测试报告模板

```markdown
# 测试报告

## 测试概述
- 测试时间: YYYY-MM-DD
- 测试环境: [环境描述]
- 测试人员: [姓名]

## 测试结果统计
| 测试类型 | 用例数 | 通过 | 失败 | 通过率 |
|----------|--------|------|------|--------|
| 功能测试 | 50 | 45 | 5 | 90% |
| 性能测试 | 10 | 9 | 1 | 90% |

## 缺陷统计
| 等级 | 数量 | 已修复 | 待修复 |
|------|------|--------|--------|
| Critical | 0 | 0 | 0 |
| Major | 2 | 1 | 1 |
| Minor | 5 | 3 | 2 |

## 测试结论
[测试结论和建议]
```

### 九、测试工具

| 工具 | 用途 | 版本 |
|------|------|------|
| pytest | 单元测试、集成测试 | 7.x |
| locust | 性能测试 | 2.x |
| playwright | E2E 测试 | 1.x |
| requests | API 测试 | 2.x |
| coverage | 代码覆盖率 | 7.x |

### 十、风险与应对

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|----------|
| 测试数据不足 | 中 | 高 | 提前准备、使用模拟数据 |
| 第三方 API 不可用 | 低 | 高 | 使用 Mock 替代 |
| 测试环境不稳定 | 中 | 中 | 准备备用环境 |
| 准确率不达标 | 中 | 高 | 增加训练数据、调整模型 |

---

## 附录：测试用例清单

### 功能测试用例

| 编号 | 用例名称 | 优先级 | 状态 |
|------|----------|--------|------|
| TC-001 | Twitter 数据采集测试 | P0 | 待执行 |
| TC-002 | 情感分析准确率测试 | P0 | 待执行 |
| TC-003 | 事件提取准确率测试 | P0 | 待执行 |
| TC-004 | 预警触发测试 | P0 | 待执行 |
| TC-005 | 情感数据 API 测试 | P0 | 待执行 |
| TC-006 | Reddit 数据采集测试 | P0 | 待执行 |
| TC-007 | 新闻数据采集测试 | P0 | 待执行 |
| TC-008 | 数据去重测试 | P1 | 待执行 |
| TC-009 | 日报生成测试 | P1 | 待执行 |
| TC-010 | 历史数据查询测试 | P1 | 待执行 |

### 性能测试用例

| 编号 | 用例名称 | 优先级 | 状态 |
|------|----------|--------|------|
| PT-001 | API 响应时间测试 | P1 | 待执行 |
| PT-002 | 并发处理测试 | P1 | 待执行 |
| PT-003 | 数据采集性能测试 | P2 | 待执行 |
| PT-004 | 分析引擎性能测试 | P2 | 待执行 |

### 准确性测试用例

| 编号 | 用例名称 | 优先级 | 状态 |
|------|----------|--------|------|
| AT-001 | 情感分析准确率测试 | P0 | 待执行 |
| AT-002 | 事件提取准确率测试 | P0 | 待执行 |
| AT-003 | 实体识别准确率测试 | P1 | 待执行 |
| AT-004 | 去重准确率测试 | P1 | 待执行 |

---

*本文档由麻子 (Paperclip Agent) 生成，基于系统需求规格说明书。*